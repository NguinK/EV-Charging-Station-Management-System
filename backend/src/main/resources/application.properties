spring.application.name=EV_CSMS
server.port=8080

spring.datasource.url=jdbc:sqlserver://${DB_HOST}:${DB_PORT};databaseName=${DB_NAME};encrypt=false;trustServerCertificate=true;
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / HIBERNATE CONFIG
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# THYMELEAF TEMPLATE CONFIG
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

#spring.jackson.time-zone=Asia/Ho_Chi_Minh

spring.profiles.active=dev

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=locvodoi2005@gmail.com
spring.mail.password=evkx kfzl iqhu wrkn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Super Admin Configuration
# IMPORTANT: In production, password MUST be set via environment variable SUPER_ADMIN_PASSWORD
app.super-admin.enabled=true
app.super-admin.email=${SUPER_ADMIN_EMAIL:superadmin@evcharging.com}
app.super-admin.password=${SUPER_ADMIN_PASSWORD}
app.super-admin.full-name=${SUPER_ADMIN_FULLNAME:Super Administrator}

